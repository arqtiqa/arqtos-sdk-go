{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://arqtiqa.github.io/arqtos/schema/witness.v1.schema.json",
  "title": "Witness v1 \u2014 an append-only signature or ratification",
  "description": "Witnesses sit OUTSIDE the signed body. ratified_at derives from the accepted witness and its time source; it is never authored on the act.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "kind",
    "act_body_id",
    "witness_kind",
    "principal",
    "key_id",
    "signature",
    "at"
  ],
  "properties": {
    "schema_version": {
      "type": "string",
      "const": "1",
      "description": "An integer carried as its canonical decimal string. The canonical encoding forbids JSON numbers."
    },
    "kind": {
      "type": "string",
      "const": "witness"
    },
    "act_body_id": {
      "type": "string",
      "minLength": 1
    },
    "witness_kind": {
      "type": "string",
      "enum": [
        "signature",
        "ratification"
      ]
    },
    "principal": {
      "type": "string",
      "minLength": 1
    },
    "key_id": {
      "type": "string",
      "description": "A verifier must establish whether this key was valid WHEN THE SIGNATURE WAS MADE. A signature without historical key status proves only that some key signed some bytes."
    },
    "signature": {
      "type": "string"
    },
    "at": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "at",
        "authority"
      ],
      "description": "When the witness was made, WITH its authority and that authority's clock provenance. keyhistory answers 'was this signature valid when it was made' from this field, so a bare timestamp answers it from a guess.",
      "properties": {
        "at": {
          "type": "string",
          "format": "date-time"
        },
        "authority": {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "name",
            "provenance"
          ],
          "properties": {
            "name": {
              "type": "string",
              "minLength": 1
            },
            "provenance": {
              "type": "string",
              "enum": [
                "unspecified",
                "local",
                "synchronised",
                "attested"
              ]
            }
          }
        }
      }
    }
  }
}
